{% extends to_extend %}

{% block content %}
    <div class="flex flex-col gap-4 justify-center items-center mt-10 sm:mx-auto sm:w-full sm:max-w-sm">
        <h2 class="text-2xl font-semibold">You did it!</h2>
        <p>{{ message }}</p>
        {% if link_back %}
            <p class="mt-10 text-center text-sm text-gray-500">
                <a href="/sign-in" class="font-semibold leading-6 text-indigo-600 hover:text-indigo-500">Log in.</a>
            </p>
        {% endif %}
    </div>

{% endblock content %}
